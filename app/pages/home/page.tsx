import HomeInfo from '@/app/components/HomeInfo';

/**
 * Renders the information on the home page.
 * @returns Home of the website.
 */
const HomePage = () => {
    return (
        <HomeInfo />
    )
};

export default HomePage;
